[sound]
path = sounds/snd_ominous_music.wav

[triggers]
words = proceed
case_sensitive = false
; List of Windows Virtual-Key Codes that act as word boundary triggers.
; You can use standard virtual key code names (e.g., VK_SPACE, VK_RETURN, VK_TAB)
; or hex codes (e.g., 0x20, 0x0D, 0x09).
boundary_keys = VK_SPACE, VK_RETURN, VK_TAB

[debug]
enabled = false
